"""
Setup script for AicodeX (backward compatibility).

For modern installations, use pyproject.toml with:
    pip install -e .
"""

from setuptools import setup

# Configuration is in pyproject.toml
setup()
